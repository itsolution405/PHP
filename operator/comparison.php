<?php
	
    $x = "3";
    $y = 3;

	var_dump($x == $y); // Menampilkan bool(true)
	var_dump($x === $y); // Menampilkan bool(false)
	var_dump($x != $y); // Menampilkan bool(false)
	var_dump($x <> $y); // Menampilkan bool(false)
	var_dump($x !== $y); // Menampilkan bool(true)
	
?>